<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Peminjaman Lab</title>

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css"
      integrity="sha512-+4zCK9k+qNFUR5X+cKL9EIR+ZOhtIloNl9GIKS57V1MyNsYpYcUrUeQc9vNfzsWfV28IaLL3i96P9sdNyeRssA=="
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/pengajuanpeminjamanLabStyle.css" />
  </head>
  <body>
    <!-- Input Informasi -->
    <div class="input_informasi">
      <div class="back_btn">
        <a href="/"><i class="fa fa-arrow-left"></i> </a>
      </div>
      <div class="wrapper">
        <h1>Peminjaman Lab</h1>
        <span class="title_span">Gunakan form dibawah ini untuk melakukan peminjaman lab</span>
        <form action="/pengajuan/peminjaman/lab" method="post">
          <div class="txt_field">
            <% if (user != null) { %>
            <input type="text" name="nama" value="<%= user.username %>" required />
            <% } else { %>
            <input type="text" name="nama" required />
            <% } %>
            <span></span>
            <label for="name">Nama</label>
          </div>
          <div class="num_field">
            <% if (user != null) { %>
            <input type="number" name="nis" value="<%" ="user.nis" % /> required> <% } else { %>
            <input type="number" name="nis" required />
            <% } %>
            <span></span>
            <label for="nis">NIS</label>
          </div>

          <div class="date_time">
            <div class="date_field">
              <p id="waktu">Waktu Peminjaman</p>
              <input type="date" name="waktu" required />
              <span></span>
            </div>
            <div class="time_field1">
              <input type="time" name="waktu" required />
              <span></span>
              <p id="koma">,</p>
              <p id="sd">s.d</p>
            </div>
            <div class="time_field2">
              <input type="time" name="waktu" required />
              <span></span>
            </div>
          </div>
          <div class="btn">
            <span></span>
            <button type="submit" class="btn_success">Kirim</button>
          </div>
          <div class="login">
            <p>Sudah punya akun? <span id="login" onclick="toogle()">Login</span></p>
          </div>
        </form>
      </div>
    </div>
    <!-- Input Inventaris -->

    <!--Header-->
    <%- include('../header') %>
    <!--Header-->

    <script>
      var state = false;
      function toogle() {
        if (state) {
          document.getElementById("login").setAttribute("type", "login");
          state = false;
        } else {
          /*go to loginPage.html*/
          state = true;
        }
      }
    </script>
  </body>
</html>
